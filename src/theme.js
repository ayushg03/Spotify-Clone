import { createTheme} from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    type: 'dark',
    background: {
      default:'#121212', // Set your desired black background color
    },
  },
});

export default theme;
